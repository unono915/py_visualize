# WORK_PROGRESS

## 목적

- 컨텍스트 윈도우가 초기화되어도 작업을 이어갈 수 있도록, 완료한 작업 단위마다 현재 상태를 기록한다.
- `PROJECT_CONTEXT.md`는 기준 문서로 유지하고, 이 문서는 실제 진행 로그와 다음 작업의 연결 지점에 집중한다.

## 현재 제품 방향

- 제품 정체성: 수업용 변수 변화 시각화 도구
- 우선순위: 초급 파이썬 문법을 모션 그래픽으로 직관적으로 이해시키는 UX
- 비우선순위: 범용 디버거 수준의 복잡한 참조 그래프나 고급 파이썬 기능

## 작업 규칙

1. 작업 하나를 끝낼 때마다 이 문서와 `PROJECT_CONTEXT.md`를 함께 갱신한다.
2. 각 기록에는 목표, 변경 파일, 테스트 결과, 다음 작업을 남긴다.
3. 테스트를 통과하지 못한 작업은 완료로 기록하지 않는다.

## 진행 로그

### Task 001

- 날짜: 2026-04-06
- 목표: 작업 기록 체계를 고정하고 `input()` AST 변환 안정성 문제를 해결한다.
- 변경 파일:
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 진행 로그 문서를 추가해 이후 작업을 이어갈 기준점을 만들었다.
  - `input()` 호출을 무조건 `await`로 바꾸던 로직을 수정했다.
  - 일반 `def` 함수 내부의 `input()`은 더 이상 문법 오류를 만들지 않고, 명시적인 지원 범위 안내 메시지로 실패한다.
- 테스트:
  - `node --check visualizer.js`
  - Python AST 검증 스크립트로 다음 케이스 확인
  - 최상위 `input()` 사용: 컴파일 성공
  - `async def` 내부 `input()` 사용: 컴파일 성공
  - 일반 `def` 내부 `input()` 사용: 지원 불가 메시지로 차단
- 결과:
  - 기존의 `await outside async function` 형태의 불친절한 실패를 제거했다.
  - 현재 학습 도구의 지원 범위를 코드 수준에서 명확히 드러내도록 바꿨다.
- 다음 작업:
  - Step snapshot 간 diff 모델을 추가해 `생성 / 변경 / 읽기 / 출력 / 입력` 장면 데이터로 변환한다.

### Task 002

- 날짜: 2026-04-06
- 목표: Step 기반 변수 생성/변경 diff를 계산하고 카드 애니메이션을 실제 UI에 연결한다.
- 변경 파일:
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 각 실행 스텝의 `globals` 스냅샷을 이전 스텝과 비교하는 diff 계산 로직을 추가했다.
  - diff 결과를 기반으로 변수 카드에 `NEW`, `CHANGED` 배지와 `anim-create`, `anim-update` 클래스를 연결했다.
  - 기존에 CSS만 있던 변수 생명주기 애니메이션이 실제 Step Explorer 동작과 연결되기 시작했다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 순수 함수 검증으로 `created / updated / deleted` 분류 확인
  - `created`: 새 변수 등장
  - `updated`: 같은 이름의 값 변경
  - `deleted`: 이전 스텝에만 존재한 변수 제거
- 결과:
  - Step을 이동할 때 어떤 변수가 새로 생겼고 어떤 값이 바뀌었는지 시각적으로 바로 드러난다.
  - 이후 연결선, 입력 토큰, 출력 흐름 같은 모션 기능을 붙일 수 있는 기반이 마련됐다.
- 다음 작업:
  - Play / Pause / 속도 조절이 있는 재생형 Step Player를 추가한다.

### Task 003

- 날짜: 2026-04-06
- 목표: Step Explorer를 수동 탐색 도구에서 재생형 학습 플레이어로 확장한다.
- 변경 파일:
  - `index.html`
  - `style.css`
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - Step Explorer에 `재생 / 일시정지` 버튼과 재생 속도 선택 UI를 추가했다.
  - 자동 재생 상태, 타이머, 속도 값을 상태 객체로 관리하도록 확장했다.
  - 수동 이동과 자동 재생이 충돌하지 않도록 탐색 버튼, 슬라이더, 예제 전환, 코드 재실행 시 자동 재생을 정지하도록 정리했다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 상태 테스트로 재생 시작 / 정지 / 속도 변경 검증
  - 마지막 스텝에서 재생 시 첫 스텝부터 다시 시작하는지 확인
- 결과:
  - Step Explorer가 교사가 설명 흐름을 통제할 수 있는 재생형 플레이어로 바뀌기 시작했다.
  - 다음 단계에서 연결선, 입력 토큰, 출력 흐름을 자동 재생 위에 자연스럽게 얹을 수 있다.
- 다음 작업:
  - `input()` 값 이동 토큰과 읽기/출력 연결선을 본 앱에 이식한다.

### Task 004

- 날짜: 2026-04-06
- 목표: 변수 시각화 영역을 카드 목록에서 장면형 캔버스로 재설계한다.
- 변경 파일:
  - `index.html`
  - `style.css`
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 오른쪽 패널을 더 넓게 쓰도록 레이아웃 비중을 조정하고, 시각화 영역을 `visualization-stage` 기반의 넓은 캔버스로 재구성했다.
  - 변수 카드를 큰 정보 패널이 아니라 작은 노드처럼 보이도록 축소하고, 자료형에 따라 서로 다른 실루엣과 밀도를 갖도록 스타일을 바꿨다.
  - 실행 중인 코드 줄과 관련 변수 노드를 SVG 연결선으로 이어 주는 오버레이를 추가했다.
  - 변수는 더 이상 그리드 칸을 꽉 채우지 않고, 함수 / 컬렉션 / 스칼라 타입별 존에 흩어져 배치되도록 레이아웃 계산 로직을 넣었다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 순수 함수 검증으로 장면 배치(`buildSceneLayout`)와 연결 대상 추출(`buildSceneConnectionTargets`) 확인
  - 함수 노드는 위쪽, 스칼라는 아래쪽, dict는 컬렉션보다 왼쪽 존에 배치되는지 확인
  - 현재 줄에서 참조된 변수와 diff 기반 업데이트 변수가 연결 대상으로 잡히는지 확인
- 결과:
  - 시각화가 “카드 목록”에서 “변수가 떠 있는 장면”으로 방향 전환됐다.
  - 사용자 피드백대로 변수 노드가 더 작아졌고, 넓은 공간을 활용하면서 코드 줄과 시각적으로 이어지는 구조를 갖추기 시작했다.
- 다음 작업:
  - `input()` 값 토큰 이동과 `print()` 흐름 선을 같은 장면 시스템 위에 추가한다.

### Task 005

- 날짜: 2026-04-07
- 목표: 연결선의 출발점을 왼쪽 에디터가 아니라 우측 장면 내부 실행 줄 토큰으로 바꾸고, 변수 노드 겹침을 줄인다.
- 변경 파일:
  - `index.html`
  - `style.css`
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 우측 시각화창 상단에 현재 실행 줄을 보여주는 `scene-code-line` 레이어를 추가했다.
  - 연결선은 이제 왼쪽 에디터 줄 번호가 아니라, 우측 실행 줄 안에서 실제 변수 이름이 표시된 토큰에서 출발한다.
  - 같은 변수가 한 줄에 여러 번 나오면 각 토큰 위치를 따로 반영하도록 바꿨다.
  - 따옴표 안 문자열은 변수 참조로 잘못 인식하지 않도록 간단한 문자열 스캐너를 넣었다.
  - 변수 노드는 렌더 후 실제 크기를 기준으로 충돌 해소 단계를 거쳐 겹침을 줄이도록 했다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 순수 함수 테스트
  - `buildSceneLineParts`: 같은 변수의 첫 등장만 `update/create`, 이후 등장은 `read`로 분류되는지 확인
  - 따옴표 안 문자열이 변수 참조로 오인되지 않는지 확인
  - `buildSceneConnectionTargets`: 반복 변수 참조가 토큰 단위로 유지되는지 확인
  - `resolveSceneNodeCollisions`: 동일 위치에 놓인 노드가 서로 다른 위치로 분리되는지 확인
- 결과:
  - 사용자가 원한 대로 “실행 중인 줄이 우측 장면에 뜨고, 그 줄의 변수 이름에서 선이 나가는 구조”가 구현됐다.
  - 단순 배치만으로 생기던 노드 겹침도 렌더 후 보정 단계로 완화됐다.
- 다음 작업:
  - `input()` 값 토큰 이동과 `print()` 출력 흐름도 같은 토큰-노드-콘솔 장면 시스템으로 확장한다.

### Task 006

- 날짜: 2026-04-07
- 목표: 코드 에디터에 단계 실행 진입용 `한 줄 실행` 버튼을 추가한다.
- 변경 파일:
  - `index.html`
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 실행 버튼 옆에 `한 줄 실행` 버튼을 추가했다.
  - 일반 `실행`은 기존처럼 전체 실행 후 마지막 스텝을 보여준다.
  - `한 줄 실행`은 같은 step 데이터 수집 구조를 사용하되, 초기 진입 위치만 첫 스텝으로 열리도록 바꿨다.
  - 엔진 로딩 상태와 실행 중 상태에 맞춰 `한 줄 실행` 버튼도 함께 활성화/비활성화되도록 묶었다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 테스트로 `setupStepExplorer()`가
  - 일반 실행일 때 마지막 스텝으로 여는지 확인
  - `initialStepIndex: 0` 옵션일 때 첫 스텝으로 여는지 확인
- 결과:
  - 사용자는 이제 왼쪽 에디터에서 `한 줄 실행`을 눌러 첫 줄부터 단계적으로 수업을 시작할 수 있다.
  - 이후 진행은 오른쪽 Step Explorer의 `다음` 버튼으로 이어지는 흐름이 만들어졌다.
- 다음 작업:
  - `input()` 값 토큰 이동과 `print()` 출력 흐름을 같은 단계 실행 흐름 위에 연결한다.

### Task 007

- 날짜: 2026-04-07
- 목표: 변수 노드를 마우스로 드래그해 사용자가 직접 배치를 조정할 수 있게 만든다.
- 변경 파일:
  - `style.css`
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 변수 노드에 드래그 상태(`sceneNodeOverrides`)를 추가해 사용자가 옮긴 위치를 step 이동 중에도 유지하도록 했다.
  - 노드 렌더 시 자동 배치 결과보다 사용자 override 위치를 우선 적용하도록 변경했다.
  - 포인터 기반 드래그 시작 / 이동 / 종료 로직을 추가하고, 드래그 중에는 연결선이 즉시 다시 그려지도록 했다.
  - 커서, 선택 방지, 드래그 중 강조 스타일을 추가해 조작감을 보완했다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 헬퍼 테스트
  - override 위치 조회와 fallback 위치 조회 확인
  - 포인터 좌표에서 scene 퍼센트 위치 계산 확인
  - 드래그 이동 시 CSS 좌표와 override 상태가 함께 갱신되는지 확인
  - 드래그 종료 시 active drag 상태가 해제되는지 확인
- 결과:
  - 생성된 변수 노드를 사용자가 직접 잡아서 원하는 위치로 재배치할 수 있는 기반이 생겼다.
  - 연결선도 새 위치를 따라가므로, 수업 중 교사가 화면 구성을 직접 정리하는 방식이 가능해졌다.
- 다음 작업:
  - `input()` 값 토큰 이동과 `print()` 출력 흐름을 현재 드래그 가능한 장면 시스템 위에 연결한다.

### Task 008

- 날짜: 2026-04-07
- 목표: `while True` 같은 무한 반복으로 페이지가 멈추기 전에 실행을 중단하는 보호 장치를 추가한다.
- 변경 파일:
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - Python 실행용 tracer에 실행 이벤트 수 제한과 경과 시간 제한을 추가했다.
  - 제한을 넘기면 내부 traceback 대신 학습용 안내 메시지로 중단하도록 `__ExecutionLimitError` 분기를 넣었다.
  - Pyodide 초기 preload import에도 `time`을 추가해 실행 시간 측정을 가능하게 했다.
- 테스트:
  - `node --check visualizer.js`
  - Node 기반 검증으로 runner script에 guard 상수와 custom exception handler가 들어가는지 확인
  - Python standalone tracer 시뮬레이션으로 `while True: pass`가 `ExecutionLimitError`로 중단되는지 확인
- 결과:
  - 무한 반복문으로 페이지 전체가 멈추는 상황을 현재 구조 안에서 상당 부분 예방할 수 있게 됐다.
  - 사용자에게는 브라우저 오류보다 “반복 조건을 확인해 달라”는 친절한 메시지가 보이게 된다.
- 다음 작업:
  - `input()` 값 토큰 이동과 `print()` 출력 흐름을 현재 안전장치 위에서 장면 시스템에 연결한다.

### Task 009

- 날짜: 2026-04-07
- 목표: 출력문이나 대입문 안의 f-string에서도 `{...}` 내부 변수를 인식해 연결선이 이어지도록 만든다.
- 변경 파일:
  - `visualizer.js`
  - `PROJECT_CONTEXT.md`
  - `WORK_PROGRESS.md`
- 구현 내용:
  - 우측 scene code line 토큰 파서를 일반 문자열과 f-string으로 구분하도록 확장했다.
  - `f'...'`, `f"..."`, `rf'...'`, `fr"..."` 같은 formatted string prefix를 감지하고, 문자열 본문 안의 `{...}` 표현식을 별도로 다시 토큰화하도록 변경했다.
  - f-string 내부 표현식에서는 기존 변수 참조 규칙과 tone 규칙을 그대로 재사용해 `read` / `update` / `create` 연결선이 이어지게 했다.
  - 일반 문자열은 기존처럼 통째로 text로 유지해서 `'name'` 같은 리터럴이 변수로 오인되지 않도록 했다.
- 테스트:
  - `node --check visualizer.js`
  - Node VM 기반 파서 검증
  - `print(f'{i} 더하는 중... 합계: {total}')`에서 `i`, `total`이 ref 토큰으로 추출되는지 확인
  - `print('name', name)`에서 따옴표 안 `name`은 무시되고 바깥 `name`만 ref로 잡히는지 확인
  - `print(f'{{합계}} {total}')`에서 escaped brace는 text로 유지되고 `total`만 ref로 잡히는지 확인
- 결과:
  - 이제 `print(f'{i} 더하는 중... 합계: {total}')`처럼 수업에서 자주 나오는 f-string 출력문도 우측 실행 줄에서 변수 이름이 강조되고 연결선이 정상적으로 이어진다.
  - 일반 문자열 처리 동작은 유지해서 기존 연결선 규칙이 깨지지 않도록 했다.
- 다음 작업:
  - `input()` 값 토큰 이동과 `print()` 출력 흐름을 장면 시스템에 연결한다.
